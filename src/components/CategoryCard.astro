---
import type { CuisineName } from "../types/cuisines";
import { cuisines } from "../utils/cuisine";
import { toCamelCaseAndRemoveSpecialChars } from "../utils/string";

export interface Props {
  name: string;
  count?: number;
  type: "cuisine" | "location";
  href?: string;
  color?: string;
  noBgColor?: boolean;
}

const {
  name,
  count,
  type,
  href = "#",
  color = "var(--color-primary)",
  noBgColor = false,
} = Astro.props;

let image = "";

const cuisine = cuisines[toCamelCaseAndRemoveSpecialChars(name) as CuisineName];
if (type === "cuisine") {
  image = "/images/reviews" + cuisine?.url || "";
}

const headerBg = cuisine?.url
  ? noBgColor
    ? "transparent"
    : color
  : "transparent";
const headerTextColor =
  cuisine?.url && !noBgColor ? "var(--color-white)" : "var(--color-gray-900)";
---

<div class:list={["category-card", image ? "has-image" : "no-image"]}>
  <a href={href} class="category-card__link">
    <div>
      <div
        class="category-card__content"
        style={image ? `background-image: url(${image});` : ""}
      >
      </div>

      <div
        class="category-card__header"
        style={`background-color: ${headerBg}; color: ${headerTextColor};`}
      >
        <h3 class="category-card__name">{name}</h3>
        {
          count && (
            <p class="category-card__count">
              {count} {count === 1 ? "review" : "reviews"}
            </p>
          )
        }
      </div>
    </div>
  </a>
</div>

<style lang="scss">
  .category-card {
    overflow: hidden;
    transition: box-shadow var(--transition-normal);

    &.no-image {
      --category-card-color: var(--color-gray-900);
      border: 1px solid var(--color-gray-900);

      .category-card__content {
        min-height: 0;
        background-color: transparent;
      }

      .category-card__header {
        position: static;
        background-color: transparent;
        color: var(--color-gray-900);
        text-align: left;
      }
    }
  }

  .category-card__image {
    width: 100%;
    height: 20rem;
    object-fit: cover;
    filter: grayscale(100%);

    transition: filter var(--transition-normal);

    &:hover {
      filter: grayscale(0%);
    }
  }

  .category-card__link {
    display: block;
    text-decoration: none;
    color: inherit;
    position: relative;
  }

  .category-card__content {
    position: relative;
    z-index: 1;
    background-size: cover;
    background-position: center; /* Center the image */

    transition: filter var(--transition-normal);

    &:hover {
    }
  }

  .has-image .category-card__content {
    height: 20rem;
  }

  .has-image .category-card__header {
    position: absolute;
    z-index: 1;
    bottom: var(--spacing-4);
    left: var(--spacing-4);
  }

  .category-card__header {
    padding: var(--spacing-4);
    text-align: center;
  }

  .category-card__name {
    margin: 0 0 var(--spacing-2) 0;
    font-size: var(--font-size-lg);
    font-weight: var(--font-weight-semibold);
    color: var(--category-card-color);
  }

  .category-card__count {
    margin: 0;
    font-size: var(--font-size-sm);
    color: var(--category-card-color);
  }
</style>
